---
# ============================================================
# Farm Sonar — selene custom stdlib for ox_lib globals.
# ============================================================

base: lua52+fivem

globals:
  lib: { any: true }
  locale:
    args:
      - type: string
      - { type: any, required: false }
